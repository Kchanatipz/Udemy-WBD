<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Products</title>
    <style>
      li {
        margin-bottom: 10px;
      }
      #details {
        padding: 5px;
        background-color: #4e97cb;
        color: white;
        border: none;
        cursor: pointer;
        border-radius: 20px;
      }
      #create {
        padding: 10px;
        background-color: #4caf50;
        color: white;
        border: none;
        cursor: pointer;
      }
      #home {
        padding: 10px;
        background-color: #f18808;
        color: white;
        border: none;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <h2><%= category %> Products</h2>
    <h4>click each product to see more</h4>
    <ul>
      <% for( let pd of products ) { %>
      <li>
        <%= pd.name %> - <i>$ <%= pd.price %></i>
        <a href="/products/<%= pd._id %>"
          ><button id="details">See more</button></a
        >
      </li>
      <% } %>
    </ul>
    <a href="/products/new"><button id="create">Create new product</button></a>
    <% if (category !== "All") { %>
    <a href="/products"><button id="home">See all products</button></a>
    <% } %>
  </body>
</html>
